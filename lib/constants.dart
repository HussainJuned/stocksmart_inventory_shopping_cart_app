/// App-wide constants shared across screens and widgets.
library;

/// Default auto-dismiss duration for SnackBars (e.g. cart add/remove
/// confirmations). Change this in one place to retune every SnackBar at once.
const Duration kSnackBarDuration = Duration(seconds: 3);
